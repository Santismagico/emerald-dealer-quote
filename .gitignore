node_modules/
dist/
dev-dist/
*.local
.env
.env.*
*.log
.DS_Store
Thumbs.db

# Paquetes de auditoria locales (no subir)
*.zip

# Mapa de conocimiento Graphify (generado, se reconstruye con /graphify)
graphify-out/

# Configuración local de Fable/Claude (permisos de esta máquina; nunca publicar)
.claude/settings.local.json
